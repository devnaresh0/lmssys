name: lms
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/illustration.png
    - assets/images/logo.png
    - assets/images/bglogo.png

    
    # or if you are using SVG
    # - assets/images/tech_illustration.svg
